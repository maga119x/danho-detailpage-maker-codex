interface:
  display_name: "Danho Detailpage Workflow"
  short_description: "Run the Korean detail-page pipeline with references."
  default_prompt: "Use $danho-detailpage-workflow to initialize AGENT.MD, analyze reference designs when supplied, and run the detail-page workflow in order."

policy:
  allow_implicit_invocation: true
