interface:
  display_name: "Danho Detailpage Workflow"
  short_description: "Run the Korean hybrid detail-page pipeline."
  default_prompt: "Use $danho-detailpage-workflow to run the detail-page workflow in order."

policy:
  allow_implicit_invocation: true
