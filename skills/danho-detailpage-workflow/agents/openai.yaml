interface:
  display_name: "Danho Detailpage Workflow"
  short_description: "Initialize and run the Korean detail-page pipeline."
  default_prompt: "Use $danho-detailpage-workflow to initialize AGENT.MD when needed and run the detail-page workflow in order."

policy:
  allow_implicit_invocation: true
