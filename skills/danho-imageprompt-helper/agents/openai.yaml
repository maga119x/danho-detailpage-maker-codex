interface:
  display_name: "Danho Image Prompt Helper"
  short_description: "Create native image prompts with style anchors."
  default_prompt: "Use $danho-imageprompt-helper to create prompts from image-plan and reference design anchors, then generate section images only through built-in Codex image_gen.imagegen GPT Image 2.0."

policy:
  allow_implicit_invocation: true
